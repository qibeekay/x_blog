import React from 'react';
import { HeroSection, Navbar } from '..';

const Header = () => {
	return (
		<header className=''>
			<Navbar />
			<HeroSection />
		</header>
	);
};

export default Header;
